import { InnerBanner } from "@/web/common/InnerBanner";

/** Inner-page hero for About us — copy lives here so the page stays a pure call. */
export function AboutBanner() {
  return (
    <InnerBanner
      title="About Bandhan"
      description="Making Every Pakistani Wedding Effortless"
    />
  );
}
